# 作業盤の中身（暗号文だけ）

このブランチには、作業盤の中身を**合言葉で暗号化した文字列だけ**が入っています。
合言葉が無ければ読めません。人名・依頼の中身はそのままの形では入っていません。
自動で15分ごとに置き換わります。

- 作っているのは `.github/workflows/board-data.yml`（main ブランチ）
- 置いてあるのは `board-data.json` / `README.md` / `.nojekyll` の3つだけ
