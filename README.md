# 作業盤の中身（暗号文だけ）

このリポジトリには、作業盤の中身を**合言葉で暗号化した文字列だけ**が入っています。
合言葉が無ければ読めません。人名・依頼の中身はそのままの形では入っていません。
自動で15分ごとに置き換わります。

- 作っているのは private リポジトリ `kagemusha-demo` の `.github/workflows/board-data.yml`
- 置いてあるのは `board-data.json` / `README.md` / `.nojekyll` の3つだけ
